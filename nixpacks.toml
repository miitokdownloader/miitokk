# Fallback 1: Set Railway variable NIXPACKS_PKGS=ffmpeg if aptPkgs below doesn't work
[phases.setup]
aptPkgs = ["ffmpeg"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn app:app"
